services:
  rdcontext:
    build: .
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    volumes:
      - rdcontext_data:/data
    ports:
      - "3000:3000"
    restart: unless-stopped

volumes:
  rdcontext_data:

